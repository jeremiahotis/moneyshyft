{
  "name": "moneyshyft-frontend-compat",
  "private": true,
  "scripts": {
    "dev": "pnpm -C ../apps/app dev",
    "build": "pnpm -C ../apps/app build",
    "build:prod": "pnpm -C ../apps/app build:prod",
    "preview": "pnpm -C ../apps/app preview"
  }
}

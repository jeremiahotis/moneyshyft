{
  "name": "moneyshyft-api-compat",
  "private": true,
  "scripts": {
    "dev": "pnpm -C ../apps/api dev",
    "build": "pnpm -C ../apps/api build",
    "start": "pnpm -C ../apps/api start",
    "test": "pnpm -C ../apps/api test",
    "test:watch": "pnpm -C ../apps/api test:watch",
    "migrate:latest": "pnpm -C ../apps/api migrate:latest",
    "migrate:latest:test": "pnpm -C ../apps/api migrate:latest:test",
    "migrate:latest:prod": "pnpm -C ../apps/api migrate:latest:prod",
    "migrate:rollback": "pnpm -C ../apps/api migrate:rollback",
    "migrate:rollback:test": "pnpm -C ../apps/api migrate:rollback:test",
    "migrate:rollback:prod": "pnpm -C ../apps/api migrate:rollback:prod",
    "migrate:make": "pnpm -C ../apps/api migrate:make",
    "seed:run": "pnpm -C ../apps/api seed:run",
    "seed:run:test": "pnpm -C ../apps/api seed:run:test",
    "seed:run:prod": "pnpm -C ../apps/api seed:run:prod"
  }
}
